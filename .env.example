# Fase 2 (opcional): URL del backend con detección automática por IA.
# Si se deja vacío, la app procesa los vídeos 100% en el navegador (ffmpeg.wasm).
VITE_API_URL=

# Tamaño máximo de archivo en bytes (500 MB por defecto)
VITE_MAX_FILE_SIZE=524288000
